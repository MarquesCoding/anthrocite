Drop app screenshots here. The landing page looks for:
  dashboard.png   (wide, shown full-width)
  menu.png        (menu-bar dropdown)
  sessions.png    (sessions view)
Any that are missing are hidden automatically. Edit the `screenshots`
array in apps/web/src/App.tsx to add/rename.
